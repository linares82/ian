<?php

class Cs_accidentesTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('cs_accidentes')->truncate();

		$cs_accidentes = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('cs_accidentes')->insert($cs_accidentes);
	}

}
