<?php

class R_documentosTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('r_documentos')->truncate();

		$r_documentos = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('r_documentos')->insert($r_documentos);
	}

}
