<?php

class ChecksTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('checks')->truncate();

		$checks = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('checks')->insert($checks);
	}

}
