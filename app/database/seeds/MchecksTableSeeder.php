<?php

class MchecksTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('mchecks')->truncate();

		$mchecks = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('mchecks')->insert($mchecks);
	}

}
