<?php

class Bit_doc_accidentesTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('bit_doc_accidentes')->truncate();

		$bit_doc_accidentes = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('bit_doc_accidentes')->insert($bit_doc_accidentes);
	}

}
