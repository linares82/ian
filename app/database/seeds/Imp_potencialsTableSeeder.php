<?php

class Imp_potencialsTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('imp_potencials')->truncate();

		$imp_potencials = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('imp_potencials')->insert($imp_potencials);
	}

}
