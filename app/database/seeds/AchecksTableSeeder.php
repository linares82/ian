<?php

class AchecksTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('achecks')->truncate();

		$achecks = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('achecks')->insert($achecks);
	}

}
