<?php

class Imp_realsTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('imp_reals')->truncate();

		$imp_reals = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('imp_reals')->insert($imp_reals);
	}

}
