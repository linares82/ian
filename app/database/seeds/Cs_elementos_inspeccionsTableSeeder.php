<?php

class Cs_elementos_inspeccionsTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('cs_elementos_inspeccions')->truncate();

		$cs_elementos_inspeccions = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('cs_elementos_inspeccions')->insert($cs_elementos_inspeccions);
	}

}
