<?php

class Cs_cat_docsTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('cs_cat_docs')->truncate();

		$cs_cat_docs = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('cs_cat_docs')->insert($cs_cat_docs);
	}

}
