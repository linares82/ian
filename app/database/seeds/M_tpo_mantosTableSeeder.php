<?php

class M_tpo_mantosTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('m_tpo_mantos')->truncate();

		$m_tpo_mantos = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('m_tpo_mantos')->insert($m_tpo_mantos);
	}

}
