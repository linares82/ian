<?php

class ChecklsTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('checkls')->truncate();

		$checkls = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('checkls')->insert($checkls);
	}

}
