<?php

class M_estatusesTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('m_estatuses')->truncate();

		$m_estatuses = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('m_estatuses')->insert($m_estatuses);
	}

}
