<?php

class ProbabilidadsTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('probabilidads')->truncate();

		$probabilidads = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('probabilidads')->insert($probabilidads);
	}

}
