<?php

class A_procedimientosTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('a_procedimientos')->truncate();

		$a_procedimientos = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('a_procedimientos')->insert($a_procedimientos);
	}

}
