<?php

class Estatus_requisitosTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('estatus_requisitos')->truncate();

		$estatus_requisitos = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('estatus_requisitos')->insert($estatus_requisitos);
	}

}
