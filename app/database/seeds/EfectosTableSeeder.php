<?php

class EfectosTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('efectos')->truncate();

		$efectos = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('efectos')->insert($efectos);
	}

}
