<?php

class SubequiposTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('subequipos')->truncate();

		$subequipos = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('subequipos')->insert($subequipos);
	}

}
