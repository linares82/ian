<?php

class P_ambiental_correosTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('p_ambiental_correos')->truncate();

		$p_ambiental_correos = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('p_ambiental_correos')->insert($p_ambiental_correos);
	}

}
