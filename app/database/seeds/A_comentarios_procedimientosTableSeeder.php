<?php

class A_comentarios_procedimientosTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('a_comentarios_procedimientos')->truncate();

		$a_comentarios_procedimientos = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('a_comentarios_procedimientos')->insert($a_comentarios_procedimientos);
	}

}
