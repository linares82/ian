<?php

class P_correo_bitacorasTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('p_correo_bitacoras')->truncate();

		$p_correo_bitacoras = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('p_correo_bitacoras')->insert($p_correo_bitacoras);
	}

}
