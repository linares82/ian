<?php

class M_mantenimientosTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('m_mantenimientos')->truncate();

		$m_mantenimientos = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('m_mantenimientos')->insert($m_mantenimientos);
	}

}
