<?php

class ReversibilidadsTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('reversibilidads')->truncate();

		$reversibilidads = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('reversibilidads')->insert($reversibilidads);
	}

}
