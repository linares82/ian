<?php

class BndsTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('bnds')->truncate();

		$bnds = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('bnds')->insert($bnds);
	}

}
