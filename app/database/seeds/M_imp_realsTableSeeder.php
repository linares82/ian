<?php

class M_imp_realsTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('m_imp_reals')->truncate();

		$m_imp_reals = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('m_imp_reals')->insert($m_imp_reals);
	}

}
