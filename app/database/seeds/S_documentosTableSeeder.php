<?php

class S_documentosTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('s_documentos')->truncate();

		$s_documentos = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('s_documentos')->insert($s_documentos);
	}

}
