<?php

class Doc_proyectosTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('doc_proyectos')->truncate();

		$doc_proyectos = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('doc_proyectos')->insert($doc_proyectos);
	}

}
