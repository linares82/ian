<?php

class Ln_dossiersTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('ln_dossiers')->truncate();

		$ln_dossiers = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('ln_dossiers')->insert($ln_dossiers);
	}

}
