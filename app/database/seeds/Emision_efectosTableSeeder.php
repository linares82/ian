<?php

class Emision_efectosTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('emision_efectos')->truncate();

		$emision_efectos = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('emision_efectos')->insert($emision_efectos);
	}

}
