<?php

class A_comentarios_pendientesTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('a_comentarios_pendientes')->truncate();

		$a_comentarios_pendientes = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('a_comentarios_pendientes')->insert($a_comentarios_pendientes);
	}

}
