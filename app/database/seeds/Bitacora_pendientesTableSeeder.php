<?php

class Bitacora_pendientesTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('bitacora_pendientes')->truncate();

		$bitacora_pendientes = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('bitacora_pendientes')->insert($bitacora_pendientes);
	}

}
