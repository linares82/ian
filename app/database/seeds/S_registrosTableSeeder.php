<?php

class S_registrosTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('s_registros')->truncate();

		$s_registros = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('s_registros')->insert($s_registros);
	}

}
