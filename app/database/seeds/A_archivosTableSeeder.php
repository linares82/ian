<?php

class A_archivosTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('a_archivos')->truncate();

		$a_archivos = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('a_archivos')->insert($a_archivos);
	}

}
