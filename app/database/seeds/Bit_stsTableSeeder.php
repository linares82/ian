<?php

class Bit_stsTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('bit_sts')->truncate();

		$bit_sts = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('bit_sts')->insert($bit_sts);
	}

}
