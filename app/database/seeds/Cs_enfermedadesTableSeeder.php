<?php

class Cs_enfermedadesTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('cs_enfermedades')->truncate();

		$cs_enfermedades = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('cs_enfermedades')->insert($cs_enfermedades);
	}

}
