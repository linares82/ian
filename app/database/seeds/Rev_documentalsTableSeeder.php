<?php

class Rev_documentalsTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('rev_documentals')->truncate();

		$rev_documentals = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('rev_documentals')->insert($rev_documentals);
	}

}
