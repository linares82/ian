<?php

class A_rr_amb_docsTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('a_rr_amb_docs')->truncate();

		$a_rr_amb_docs = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('a_rr_amb_docs')->insert($a_rr_amb_docs);
	}

}
