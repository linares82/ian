<?php

class Rev_documentoal_lnsTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('rev_documentoal_lns')->truncate();

		$rev_documentoal_lns = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('rev_documentoal_lns')->insert($rev_documentoal_lns);
	}

}
