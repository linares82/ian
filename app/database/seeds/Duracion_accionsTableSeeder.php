<?php

class Duracion_accionsTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('duracion_accions')->truncate();

		$duracion_accions = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('duracion_accions')->insert($duracion_accions);
	}

}
