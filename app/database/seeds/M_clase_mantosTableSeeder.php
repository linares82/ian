<?php

class M_clase_mantosTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('m_clase_mantos')->truncate();

		$m_clase_mantos = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('m_clase_mantos')->insert($m_clase_mantos);
	}

}
