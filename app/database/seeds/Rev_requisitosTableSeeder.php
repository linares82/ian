<?php

class Rev_requisitosTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('rev_requisitos')->truncate();

		$rev_requisitos = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('rev_requisitos')->insert($rev_requisitos);
	}

}
